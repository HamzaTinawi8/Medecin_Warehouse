import 'package:flutter/material.dart';

class AllPurchasesScreen extends StatelessWidget {
  const AllPurchasesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return (Scaffold(
      body: Text('all prushes'),
    ));
  }
}
